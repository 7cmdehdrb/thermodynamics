import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  HomeView: {
    flex: 1,
    justifyContent: "center",
    alignItems: "center",
  },
  centerView: {
    flex: 1,
    justifyContent: "center",
    alignItems: "center",
  },
  eachView: {
    flex: 1,
    justifyContent: "center",
    alignItems: "center",
  },
});
